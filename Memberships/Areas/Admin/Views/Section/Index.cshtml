@model IEnumerable<Memberships.Entities.Section>
@using Memberships.Areas.Admin.Models;

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
    @Html.Partial("_CreateButtonPartial")
</p>
<table class="table table-striped table-condensed">    @*table-condensed=>zmniejsza odstepy miedzy wierszami*@
    <tr class="success">
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Title)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>*@
            @Html.Partial("_TableButtonPartial", new SmallButtonModel { Id = item.Id })
        </tr>
    }

</table>
